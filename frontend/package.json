{
  "name": "alfred-frontend",
  "version": "1.0.0",
  "description": "Alfred AI Frontend Web Application",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy-html",
    "build:production": "vite build && npm run copy-html",
    "copy-html": "copy login.html dist\\ && copy signup.html dist\\ && copy profile.html dist\\",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["frontend", "web", "alfred", "ai"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^7.1.5"
  }
}